# =============================================================================
# ENVIRONMENT VARIABLES FOR PRODUCTION DEPLOYMENT
# =============================================================================
# Copy this file to .env.local for local development
# Add these same variables to Vercel Dashboard > Project Settings > Environment Variables

# =============================================================================
# SANITY CMS CONFIGURATION (Required)
# =============================================================================
# To get your project ID and dataset:
# 1. Create a Sanity account at https://sanity.io
# 2. Create a new project or run: npx sanity init
# 3. Get your project ID from https://sanity.io/manage

NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# =============================================================================
# SANITY API TOKEN (Recommended for production)
# =============================================================================
# Create at: https://sanity.io/manage -> Your Project -> API -> Tokens
# Use a "Viewer" token for read-only access (more secure for production)
SANITY_API_READ_TOKEN=

# =============================================================================
# WEBHOOK SECRET (Required for automatic content revalidation)
# =============================================================================
# This secret authenticates webhook requests from Sanity when content changes.
# Generate a secure random string with: openssl rand -base64 32
# Add the same secret to your Sanity webhook configuration at:
# https://sanity.io/manage -> Your Project -> API -> Webhooks
SANITY_REVALIDATE_SECRET=

# =============================================================================
# VERCEL DEPLOYMENT CHECKLIST
# =============================================================================
# 1. Add all variables above to Vercel Dashboard
# 2. Enable for environments: Production, Preview, Development
# 3. Configure Sanity CORS origins for your production URL
# 4. Set up Sanity webhook pointing to: https://your-domain.com/api/revalidate
# 5. Add webhook header: x-sanity-secret: <your-SANITY_REVALIDATE_SECRET>
